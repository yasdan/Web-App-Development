To complete the game app, you need to:

Install Node.js
Unzip if required this folder. then open a terminal in your IDE (VS Code)  and cd to the directory you unzipped
Install the dependencies with the command npm install. 
Run npm start to start a local server and follow the prompts to view the code running in a browser.